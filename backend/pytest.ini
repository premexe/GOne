[pytest]
# This is a checked-in reference snapshot, not a second runnable test suite.
# Excluding it prevents pytest from importing two different tests.conftest
# modules with the same Python module name.
norecursedirs =
    gone_ai_module_repo
    .pytest_cache
