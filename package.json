{
  "name": "gone-lifelink-ai",
  "version": "1.0.0",
  "description": "LifeLink AI Emergency Health Companion",
  "scripts": {
    "backend": "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "frontend": "cd frontend && npx --node-options=\"--max-old-space-size=8192\" expo start --clear"
  }
}
